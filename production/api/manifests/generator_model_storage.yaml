apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: generator-model
  namespace: production
spec:
  storageClassName: SELECTED_STORAGE
  accessModes:
    - ReadMany
  resources:
    requests:
      storage: 1000Mi
